# DAT_Dmesg_tool
The tool is intended to do RCV-aimed searches so that it eases cycles/responses lookup and analysis as well as providing the ability of decoding the NVMe dbg driver loggings into the kernel messages, while providing a text files handling environment that reduces the need for switching to other text-processing apps by including some basic functions.
